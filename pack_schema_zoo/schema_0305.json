[{"id": "dup_27", "source": "forum_dump", "timestamp": "2024-03-30T02:29:00", "meta": [{"k": "tag", "v": "safety"}, {"k": "tag", "v": "linux"}], "title": "infra pipelines k8s pipelines k8s"}, {"id": "r305_1", "source": "helpdesk_export", "timestamp": "2024-02-02T15:30:00", "text": "fix todo note bug todo"}, {"id": "r305_2", "source": "forum_dump", "timestamp": "2024-06-04T01:32:00", "title": "debug reward guide reward guide quick", "text": "scaling ppo uses laws rewards training training laws kl policy uses for ppo kl for rewards scaling the kl scaling for ppo scaling the ppo uses the training uses uses rewards rewards for for for kl kl agent policy agent ppo kl the training uses laws kl the policy scaling laws kl ppo system the policy agent uses the rewards uses laws rewards the laws scaling laws laws training ppo agent the agent the training system kl kl scaling system policy rewards rewards agent laws system policy scaling training training system the training agent uses training scaling scaling policy ppo uses the"}, {"id": "r305_3", "source": "forum_dump", "timestamp": "2024-01-27T02:45:00", "title": "config quick reward reward policy install", "text": "scaling kl training the kl uses training scaling agent system rewards system laws laws kl the policy training system rewards for uses kl the for system kl scaling training the training laws kl for policy uses for scaling ppo ppo policy uses kl policy laws agent for laws rewards ppo laws kl training uses for uses the ppo training rewards rewards system for ppo ppo laws ppo scaling agent scaling the policy the agent agent scaling training agent ppo training agent training system agent laws training kl policy system ppo system ppo training ppo"}]