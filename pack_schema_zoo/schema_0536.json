{"id": "r536_0", "source": "legacy_csv_ingest", "timestamp": "2024-05-28T05:10:00", "title": "install policy guide install config install", "text": "training scaling training the system training rewards uses for agent ppo for for kl laws the the for training uses rewards uses system system training policy ppo scaling the uses scaling scaling agent laws training kl laws uses ppo for the system agent training ppo training the system rewards ppo scaling system the the rewards rewards rewards ppo policy laws laws policy uses laws ppo kl laws policy ppo training scaling for uses uses uses uses rewards laws scaling for policy agent scaling rewards the rewards uses ppo for scaling"}
{"id": "r536_1", "source": "forum_dump", "timestamp": "2024-08-03T19:00:00", "meta": [{"k": "tag", "v": "spark"}, {"k": "tag", "v": "eval"}, {"k": "tag", "v": "ppo"}], "title": "dagster infra dagster infra dagster"}
{"id": "r536_2", "source": "helpdesk_export", "timestamp": "2024-06-19T20:09:00", "title": "guide reward config reward gpu policy", "text": "the rewards uses ppo rewards agent rewards scaling rewards ppo uses policy system ppo rewards kl scaling uses rewards ppo the training system policy training ppo for kl training ppo training system scaling laws scaling kl system ppo ppo the uses uses laws for kl kl agent laws system laws for scaling for kl laws kl for ppo kl scaling ppo policy policy agent kl uses uses laws laws for kl scaling system training laws system the the system policy scaling ppo training kl policy system the for for policy the"}
{"id": "r536_3", "source": "news_api", "timestamp": "2024-04-20T21:00:00", "title": "reward guide debug reward install debug", "text": "for the agent laws training scaling training the for policy policy for laws agent system laws uses uses scaling laws system agent the scaling rewards the system training policy scaling scaling ppo uses policy kl kl laws laws scaling ppo agent uses for the policy laws agent uses for rewards kl ppo agent ppo laws policy uses scaling training laws laws system ppo policy training uses rewards kl for the agent agent rewards the for scaling for policy kl policy for system the policy system the rewards ppo rewards policy uses system rewards ppo uses kl rewards training rewards policy kl the system rewards system laws for rewards system for"}
