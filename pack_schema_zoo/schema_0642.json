{"id": "r642_0", "source": "news_api", "timestamp": "2024-04-19T08:50:00", "title": "config gpu reward quick guide debug", "text": "policy scaling training ppo system policy the policy training policy uses uses laws ppo laws uses uses training ppo the policy for the for system kl agent kl kl uses system training the uses the scaling policy kl system uses for policy system scaling uses kl rewards uses uses rewards for for rewards"}
{"id": "r642_1", "source": "forum_dump", "timestamp": "2024-06-23T02:43:00", "title": "config reward install policy install config", "text": "kl ppo uses uses kl ppo scaling ppo system agent kl the policy kl uses system agent rewards rewards scaling system training the kl laws laws scaling scaling the policy training scaling scaling for scaling policy for laws the policy policy uses rewards for kl the training laws ppo agent policy ppo the system the rewards training for agent uses kl training scaling policy policy laws rewards agent system system the for policy the policy the agent ppo training the training policy training for rewards for uses for rewards kl laws the agent scaling training laws the ppo ppo ppo rewards"}
