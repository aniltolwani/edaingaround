{"id": "r699_0", "source": "helpdesk_export", "timestamp": "2024-05-19T20:47:00", "title": "gpu policy config debug quick guide", "text": "system kl scaling laws policy kl the agent agent the for system laws agent the training kl ppo for for policy rewards agent for rewards the rewards laws policy agent training policy training laws agent uses rewards kl training system kl rewards scaling kl system the rewards laws uses scaling policy ppo system system policy system the for laws kl laws scaling the for uses scaling scaling kl system uses ppo for scaling training policy laws scaling uses system ppo policy training agent policy laws kl policy for system"}
{"id": "dup_19", "source": "legacy_csv_ingest", "timestamp": "2024-03-24T02:40:00", "title": "config policy gpu config config policy", "text": "uses scaling ppo uses agent training system training uses agent rewards the training rewards laws scaling system agent the policy kl kl rewards ppo kl agent training kl policy uses uses laws kl the ppo uses kl training rewards scaling laws uses laws agent for the uses for scaling laws for system"}
