{"id": "r592_0", "source": "news_api", "timestamp": "2024-05-02T06:49:00", "title": "quick quick debug policy quick gpu", "text": "kl rewards kl training uses agent ppo ppo scaling rewards ppo laws scaling ppo system agent laws kl policy for training system laws system kl policy scaling kl policy policy laws agent ppo for laws policy system laws policy laws kl the kl for scaling for agent for ppo rewards training ppo uses scaling kl training policy for kl policy laws kl kl scaling agent uses system scaling for uses rewards laws training kl for system scaling ppo scaling rewards scaling rewards laws uses system laws agent policy scaling rewards scaling laws ppo training the for uses system rewards ppo kl ppo ppo policy agent rewards kl system"}
{"id": "r592_1", "source": "forum_dump", "timestamp": "2024-07-31T16:11:00", "title": "policy config gpu reward quick config", "text": "for uses kl laws system rewards agent uses laws the kl policy uses training rewards for for rewards laws rewards agent ppo policy agent scaling policy policy ppo agent system the kl system for scaling uses rewards training agent kl for kl for rewards policy laws laws ppo kl scaling scaling agent system training policy rewards policy scaling ppo agent kl rewards the policy uses policy agent the laws scaling agent uses kl laws rewards uses for system kl for ppo laws uses laws agent scaling training training policy laws scaling ppo system training scaling policy training ppo for uses the"}
