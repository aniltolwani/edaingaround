{"id": "r135_0", "source": "legacy_csv_ingest", "timestamp": "2024-05-18T12:14:00", "docs": [{"t": "H1", "v": "dpo grad ppo ppo token dpo rlhf grad loss reset rlhf loss loss token dpo"}]}
{"id": "r135_1", "source": "forum_dump", "timestamp": "2024-02-23T18:46:00", "title": "gpu policy install install reward config", "text": "scaling policy rewards uses ppo uses kl laws uses scaling scaling uses for the uses for the ppo the training scaling ppo agent uses for ppo kl uses laws ppo training for agent system scaling kl scaling kl training laws system policy uses scaling kl for laws uses policy laws uses uses training rewards for for system system kl ppo kl kl scaling kl kl ppo laws system policy laws rewards the policy the kl policy scaling scaling kl system for uses training policy ppo policy laws kl ppo scaling system kl for policy ppo training policy the ppo system rewards laws"}
{"id": "r135_2", "source": "partner_feed", "timestamp": "2024-02-22T18:13:00", "title": "gpu guide debug gpu quick debug", "text": "policy uses system scaling rewards ppo scaling system ppo uses the kl kl laws kl scaling policy uses scaling rewards uses training laws rewards the scaling system ppo the policy the for ppo training policy uses for policy policy ppo system rewards scaling for the scaling training policy kl system policy for laws for for system training kl uses laws policy scaling uses the uses uses the kl training uses policy for agent scaling uses policy ppo the for the uses the training training policy scaling scaling laws scaling kl kl kl for scaling laws the ppo laws laws laws the for for laws laws agent agent scaling training agent system policy training"}
{"id": "r135_3", "source": "partner_feed", "timestamp": "2024-01-12T08:06:00", "content": {"header": "agent config debug debug", "body": "action reset buffer action obs step action env env reset action info step step action reset info info step done env buffer reset done env env info env obs info reset info env env action env done obs action info info buffer buffer action info action buffer buffer reset buffer obs reset reset step env info info step reset reset obs buffer info reset reset action info action obs step action done env step reset env env reset reset step buffer action done reset done step info reset buffer buffer step reset action env buffer reset info obs action reset buffer done obs done buffer info obs action step obs step buffer buffer info done env reset"}, "meta": {"tags": ["eval", "kubernetes"], "lang": "fr", "author": null, "score": null}}
