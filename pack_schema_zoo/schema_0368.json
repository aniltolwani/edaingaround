{"id": "r368_0", "source": "forum_dump", "timestamp": "2024-06-03T15:25:00", "title": "reward gpu guide install config gpu", "text": "ppo the for kl ppo training ppo uses the agent for system agent uses uses uses rewards ppo kl ppo rewards system training the scaling scaling uses uses for ppo for system ppo scaling uses training uses system training rewards agent agent system the uses ppo for policy rewards for for system scaling system agent system training policy kl agent rewards system scaling rewards agent for policy ppo scaling system uses uses ppo rewards rewards system scaling scaling scaling rewards uses policy laws scaling agent rewards laws the kl scaling scaling rewards laws agent the ppo the rewards agent kl system the policy system system kl ppo system kl policy uses for agent"}
{"id": "r368_1", "source": "helpdesk_export", "timestamp": "2024-06-09T01:33:00", "title": "install policy config gpu guide reward", "text": "agent training ppo agent rewards for the system laws agent training scaling uses kl rewards uses uses kl training system the scaling rewards policy the agent uses laws for laws uses system kl kl agent agent policy ppo training ppo laws training uses scaling policy kl policy rewards system system scaling for for agent agent scaling laws scaling the training agent uses uses the scaling policy ppo system for agent the system policy agent the laws ppo rewards agent kl system the uses ppo the agent the laws ppo"}
{"id": "r368_2", "source": "forum_dump", "timestamp": "2024-07-27T02:09:00", "text": "fix rl todo fix todo"}
