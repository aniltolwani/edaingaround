{"id": "r510_0", "source": "news_api", "timestamp": "2024-07-25T07:19:00", "title": "gpu gpu quick guide gpu quick", "text": "kl rewards training uses for scaling uses uses ppo for the ppo training kl rewards training agent agent training policy ppo rewards for the kl for agent training for scaling agent the training scaling scaling agent uses rewards kl for system uses for kl agent uses ppo scaling rewards policy for scaling kl for kl system agent training laws the scaling agent scaling ppo laws uses the uses policy kl scaling laws laws uses laws system laws system training uses laws scaling policy laws training scaling laws policy scaling scaling for policy uses for ppo scaling rewards scaling kl the for agent for agent rewards the rewards for the policy laws laws"}
{"id": "r510_1", "source": "forum_dump", "timestamp": "2024-01-07T13:44:00", "title": "gpu install debug policy config reward", "text": "rewards ppo the kl system laws scaling training for scaling training policy policy laws rewards agent kl for system the rewards agent the system scaling uses rewards agent laws policy kl agent training system kl scaling kl uses"}
