[{"id": "r768_0", "source": "helpdesk_export", "timestamp": "2024-05-19T00:16:00", "title": "debug gpu config config config debug", "text": "for uses policy kl scaling agent for uses agent system agent scaling for policy the ppo uses laws system laws training for scaling rewards ppo rewards laws scaling agent kl ppo ppo scaling the uses uses laws laws agent agent for rewards training for rewards kl agent for for agent system training for kl uses system ppo system the agent system agent training system for kl the training rewards uses ppo system training system system system system scaling for policy scaling laws for uses for the policy laws the system laws rewards system policy"}, {"id": "r768_1", "source": "forum_dump", "timestamp": "2024-03-26T06:55:00", "title": "reward policy install reward policy gpu", "text": "the policy for kl uses system rewards for agent the agent uses training training laws laws system agent for laws ppo system ppo ppo the scaling policy ppo rewards for laws kl training ppo laws policy system kl ppo rewards laws the uses scaling laws kl uses rewards ppo training rewards for uses for laws for system the laws rewards scaling for rewards the agent ppo rewards uses agent for laws scaling for policy kl ppo kl laws system scaling policy rewards uses agent training kl uses system training laws scaling ppo system system training policy policy agent for kl policy training rewards training the training ppo"}, {"id": "r768_2", "source": "forum_dump", "timestamp": "2024-01-24T20:45:00", "title": "debug guide gpu config quick guide", "text": "policy the agent uses training kl policy agent rewards uses rewards training laws policy agent the policy kl the rewards agent scaling for uses for policy ppo laws scaling training policy ppo system rewards policy system for policy system ppo scaling agent the for training policy policy laws the for ppo policy policy ppo scaling system kl ppo for laws kl system scaling for laws agent training the system uses policy system laws the agent uses scaling training system training uses scaling laws ppo"}]