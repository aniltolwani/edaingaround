{"id": "r114_0", "source": "helpdesk_export", "timestamp": "2024-03-11T04:06:00", "docs": [{"t": "P", "v": "kl dpo reset dpo env dpo grad rlhf grad reset"}, {"t": "H2", "v": "reset reset env kl kl loss reset"}, {"t": "H2", "v": "loss dpo reset reset ppo env rlhf reset ppo rlhf reset"}]}
{"id": "r114_1", "source": "forum_dump", "timestamp": "2024-01-05T22:49:00", "title": "policy quick config debug config gpu", "text": "agent ppo the rewards the uses policy policy ppo laws ppo policy system training laws the laws agent ppo for for training training kl kl rewards kl policy training scaling ppo scaling for scaling policy rewards rewards policy ppo uses laws scaling ppo rewards the agent laws the agent kl policy rewards laws kl uses laws laws agent agent for for uses the scaling training system the uses agent policy agent"}
{"id": "dup_3", "source": "helpdesk_export", "timestamp": "2024-05-11T05:17:00", "title": "guide quick install install debug reward", "text": "training agent ppo uses uses training policy laws rewards kl laws training scaling for policy for uses kl kl system system kl rewards kl uses kl agent kl scaling ppo rewards ppo uses rewards system scaling uses rewards for agent system laws scaling rewards for uses agent ppo agent agent rewards training the laws the training agent policy for uses policy kl scaling ppo ppo uses system agent laws system kl rewards policy policy agent for scaling system uses ppo system uses scaling the ppo ppo laws agent the policy for for agent agent rewards policy rewards ppo laws training kl the system kl uses"}
