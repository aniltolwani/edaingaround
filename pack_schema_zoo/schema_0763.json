{"id": "r763_0", "source": "helpdesk_export", "timestamp": "2024-04-01T12:44:00", "title": "config config gpu policy policy reward", "text": "laws ppo the scaling rewards policy ppo training policy system training agent kl agent for kl system system uses for rewards scaling the for ppo agent scaling training kl kl system system for training scaling training rewards rewards the uses rewards kl system training kl ppo laws scaling laws for agent policy"}
{"id": "r763_1", "source": "news_api", "timestamp": "2024-06-12T05:27:00", "title": "install gpu debug guide reward guide", "text": "for for policy laws for scaling training the system ppo the kl the training laws system agent policy the the training scaling laws the the scaling rewards scaling ppo uses scaling ppo rewards agent system laws the the scaling ppo kl the for laws the for agent scaling uses training agent the the ppo the policy scaling uses system rewards agent system ppo training ppo policy scaling laws the kl rewards ppo scaling agent training training training agent system the policy for kl ppo system rewards the training agent uses scaling training ppo for training system kl rewards system policy for the system rewards agent laws training uses training policy ppo system kl policy rewards laws ppo ppo rewards scaling"}
{"id": "r763_2", "source": "legacy_csv_ingest", "timestamp": "2024-04-26T21:52:00", "content": {"header": "reward reward policy reward", "body": "env step obs env done reset step obs obs obs obs reset reset info buffer obs obs env buffer buffer done info done info step env done done env obs buffer info done step info reset done reset action action buffer action obs obs done reset step obs info step step done info env info info done env buffer done obs done env env action obs obs info buffer done reset env obs done info env action buffer step buffer buffer obs done reset buffer action"}, "meta": {"tags": ["privacy", "training", "infra", "install"], "lang": "en", "author": "riley", "score": null}}
{"id": "r763_3", "source": "news_api", "timestamp": "2024-02-17T04:29:00", "title": "policy debug install install reward quick", "text": "ppo policy ppo ppo training training kl training uses policy scaling agent the the scaling laws laws the laws training scaling the kl rewards scaling training laws laws rewards for ppo agent for system rewards scaling kl system laws the uses uses policy policy rewards uses rewards uses for agent for system training kl ppo uses the laws laws uses rewards ppo uses policy ppo agent laws for policy kl ppo kl kl agent training for policy for ppo laws kl rewards policy ppo rewards training kl ppo ppo laws policy for uses training rewards uses rewards kl ppo the policy scaling kl rewards kl agent training system laws training kl system ppo training"}
