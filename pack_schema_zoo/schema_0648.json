[{"id": "r648_0", "source": "legacy_csv_ingest", "timestamp": "2024-06-28T16:42:00", "title": "policy policy config install debug policy", "text": "policy laws training policy laws ppo scaling kl policy the agent uses rewards training system ppo uses the rewards the laws system the ppo for agent policy rewards ppo the laws agent policy scaling ppo uses for uses laws laws policy rewards training scaling for ppo for agent rewards agent uses for scaling for uses agent ppo the for policy ppo agent uses ppo policy uses agent uses training scaling for for scaling for"}, {"id": "r648_1", "source": "helpdesk_export", "timestamp": "2024-06-10T19:08:00", "title": "debug quick gpu guide reward debug", "text": "the rewards rewards system system system training the the training laws ppo policy kl training ppo for system system rewards the policy uses laws ppo agent kl the training scaling uses rewards policy laws training policy scaling kl the uses the rewards system for system policy policy scaling kl uses policy system ppo for system uses rewards system laws system system kl scaling scaling uses scaling scaling uses training rewards uses policy laws policy policy laws ppo rewards"}]